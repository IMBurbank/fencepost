{
  "name": "fencepost",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": ["rust-lang.rust-analyzer", "tamasfe.even-better-toml"]
    }
  },
  "postCreateCommand": "cargo build --release 2>/dev/null || echo 'Build skipped — run cargo build manually'",
  "remoteUser": "dev"
}
