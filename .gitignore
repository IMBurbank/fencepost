target/
.DS_Store

# Docker
docker-compose.override.yml

# Claude Code — keep settings.json, ignore everything else
.claude/*
!.claude/settings.json

# Fencepost temporary override (see .fencepost-override.example)
.fencepost-override

# Transfer/session artifacts
guard-transfer*.md
*.bak.*
